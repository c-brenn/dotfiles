# == vim == #
alias vi="nvim"
alias vim="nvim"

# == tmux == #
alias tm="tmux"

# == elixir == #
alias iem="iex -S mix"
alias mps="mix phoenix.server"

# == shell == #
alias psg="ps aux | grep "
alias es="exec $SHELL"
