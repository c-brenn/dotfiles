# == Shell == #
alias ls='ls -Gp'
alias l='ls -1A'

# == vim == #
alias vi='nvim'
alias vim='nvim'

# == tmux == #
alias tm='tmux'

# == ruby == #
alias be='bundle exec'

# == docker == #
alias dc='docker-compose'
